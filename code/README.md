# Analysis scripts & notebooks

Upload Python, SQL, and data files here via the admin file terminal (`/admin`).

Supported: `.py`, `.ipynb`, `.sql`, `.r`, `.csv`

Reference them from case studies in `content/projects/`:

```markdown
Analysis script: `content/code/my-analysis.py`
```
